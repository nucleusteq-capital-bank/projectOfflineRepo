apiVersion: azmonitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: offline-repo-monitor
  namespace: capitalbank-dev
spec:
  selector:
    matchLabels:
      app: offline-repo
  podMetricsEndpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s 