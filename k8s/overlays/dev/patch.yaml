apiVersion: apps/v1
kind: Deployment
metadata:
  name: offline-repo-deployment
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: offline-repo
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
